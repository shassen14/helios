# configs/fixtures/maps/empty_20x20.toml
# A 20 m × 20 m empty map (all cells unknown → traversable for planning).

width_m      = 20.0
height_m     = 20.0
resolution_m = 0.2
origin       = [0.0, 0.0]
